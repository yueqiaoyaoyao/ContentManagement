<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="contentmanagement.mapper.UserMapper">

    <insert id="addUser" parameterType="String">
        insert into `cms`.`cms_user` (`name`,`password`)
        values (#{param1},#{param2})
    </insert>

    <select id="queryUserByName" parameterType="String" resultType="User">
        select * from `cms`.`cms_user` where name = #{param1}
    </select>

    <select id="findAll" parameterType="String" resultType="User">
        select * from `cms`.`cms_user`
    </select>

    <update id="modifyUser" parameterType="String" >
        UPDATE `cms`.`cms_user`
        SET `password` =  #{param2}
        WHERE (`name`=#{param1});
    </update>

    <delete id="deleteUser" parameterType="String">
        DELETE FROM `cms`.`cms_user`
        WHERE `name` = #{param1}
    </delete>

</mapper>